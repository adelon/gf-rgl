# Synthetic fixtures

Small JSON-array fixtures in this directory exercise envelope parsing, exact
language selection, Unicode and unusual strings, malformed input, and
contradictory source evidence without depending on the historical dictionaries.
