# Pinned-dump fixtures

After the first verified full ingest, this directory contains a small verbatim
selection of representative German Lexeme entities. Its manifest records the
snapshot SHA-256 and canonical entity hashes.
